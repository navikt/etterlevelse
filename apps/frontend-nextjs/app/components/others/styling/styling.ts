export const borderColor = (color?: string) => ({
  borderLeftColor: color,
  borderTopColor: color,
  borderRightColor: color,
  borderBottomColor: color,
})

export const borderRadius = (radius: string) => ({
  borderBottomLeftRadius: radius,
  borderBottomRightRadius: radius,
  borderTopLeftRadius: radius,
  borderTopRightRadius: radius,
})

export const borderStyle = (radius: any) => ({
  borderBottomStyle: radius,
  borderTopStyle: radius,
  borderRightStyle: radius,
  borderLeftStyle: radius,
})

export const borderWidth = (radius: string) => ({
  borderBottomWidth: radius,
  borderTopWidth: radius,
  borderRightWidth: radius,
  borderLeftWidth: radius,
})
