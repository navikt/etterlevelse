client:
    behandlingskatalog:
      base-url: http://localhost:${wiremock.server.port:8080}/bkat
    teamcat:
      url: http://localhost:${wiremock.server.port:8080}/teamcat
    slack:
      base-url: http://localhost:${wiremock.server.port:8080}/slack
    devmail:
      address: teamdatajegerne@nav.no
      slack-channel-id: C07B5KX3R0X

etterlev:
    security:
        enabled: false

spring:
    main:
        allow-bean-definition-overriding: true
        lazy-initialization: true