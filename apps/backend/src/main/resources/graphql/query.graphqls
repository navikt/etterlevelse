scalar DateTime
scalar Date

# Queries

type Query {
    krav(id: ID!): Krav
    kravForNummer(nummer: Int!, versjon: Int!): Krav
    kravFor(relevans: String): [Krav!]!

    etterlevelse(id: ID!) : Etterlevelse
}