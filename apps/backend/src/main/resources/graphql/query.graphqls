scalar DateTime
scalar Date

# Queries

type Query {
    kravById(id: ID!): Krav
    kravByNummer(nummer: Int!, versjon: Int!): Krav
    krav(relevans: String, nummer: Int): [Krav!]!

    etterlevelseById(id: ID!) : Etterlevelse
}