query getKrav($relevans: [String!], $nummer: Int, $behandlingId: String, $pageNumber: NonNegativeInt, $pageSize: NonNegativeInt) {
    krav(filter: {relevans: $relevans, nummer: $nummer, behandlingId: $behandlingId}, pageNumber: $pageNumber, pageSize: $pageSize) {
        id
        navn
    }
}
